apiVersion: v1
kind: Service
metadata:
  name: {{ include "ark-broker.fullname" . }}
  labels:
    {{- include "ark-broker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ark-broker.selectorLabels" . | nindent 4 }}